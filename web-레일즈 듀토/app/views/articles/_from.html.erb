<%= form_for @article do |f| %><%#아티클 모델과 코드 연동%>
 
  <%#공통 부분을 부분 템플릿으로 만들어 사용 이름앞에 _붙어야함%>

    <% if @article.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@article.errors.count, "error") %> prohibited
          this article from being saved:
        </h2>
        <ul>
          <% @article.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %> <%#위는 오류 낫을떄 표시하는 코드%>


    
    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>
   
    <p>
      <%= f.label :text %><br>
      <%= f.text_area :text %>
    </p>
   
    <p> 
      <%= f.submit %>
    </p>
   
  <% end %>